# =====================================================
# ELAND Platform — Production Environment Variables
# =====================================================
# Энэ файлыг .env.production нэрээр хуулж утгуудыг солино
#
# cp .env.production.example .env.production
# nano .env.production
# =====================================================

# ─── Серверийн тохиргоо ───
PUBLIC_URL=https://auction.eland.mn
# Хэрэв domain байхгүй бол IP-ээр:
# PUBLIC_URL=http://YOUR_SERVER_IP

# ─── PostgreSQL ───
POSTGRES_USER=eland_prod
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# ─── Redis ───
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_HERE

# ─── JWT ───
JWT_SECRET=CHANGE_ME_MIN_32_CHAR_SECRET_KEY_HERE_1234567890abcdef
JWT_REFRESH_SECRET=CHANGE_ME_ANOTHER_32_CHAR_SECRET_KEY_1234567890abcdef
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ─── MinIO (Object Storage) ───
MINIO_ACCESS_KEY=eland_prod_minio
MINIO_SECRET_KEY=CHANGE_ME_MINIO_SECRET_KEY_HERE

# ─── SMTP (Email) ───
# Gmail жишээ:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
# Mailgun жишээ:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@mg.eland.mn
# SMTP_PASS=your-mailgun-password
